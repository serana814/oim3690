# Session 16 & 17 - Mar 10 & Mar 12 - 



## What I Learned This Week
### Tuesday, Mar 24
- making modifications to out todo list (new javascript file and splitting the javascript and html code)
- how to select by ID: queryselector
- open console (F12): on any webpage, you can open DevTools Console and type "document.queryselector('h1')
- what is an event?
    - actions that happen on the page 
        - clicks a button
        - type in an input
        - hover over an element
        - submit a form 
    - javascript can listen to these events and respond
    - "when something happens, do this" --> what you want to code into your js (event listener) (bind the event to the element)
- 
#### Analyzing todo.html 
- where is the <script> tag?
    - the script tag is at the bottom of the <body> tag right before it closes. This lets the browser render everything in the html first, so that they can be found by javascript once it runs
- how does javascript find html elements on the page?
    - by using document.getElementById()
- what makes the "add" button actually do something?
    - It has an event listener attatched to it. Once you click add, it submits it 
- where does the code change the page? (add text, add items, etc.)?
    - it mainly changes the page in the renderTools() function 

### Thursday, Mar 26
- Javascript engine v8 is created by google and is imbedded in every engine right now
#### Review Questions
- you wrote const btn = document.querySelector('button') but clicking the button does nothing. What line of code is missing? --> the event listener
- document.querySelector('myButton') returns null, but <button id = "myButton"> is right there in the HTML, what is wrong?
- you changed the headings color in the Console and it worked. After refreshing the page it is gone. Why? --> when you refresh it re sends the request to download the html again 
#### Javascript basics
- variable declaring: use **let** fot values that change, **const** for values that stay the same
- variable naming: start with letter, _, or $ (no spaces or special characters)
- Data Types
    - Primitive Types: string, number, boolean, undefined, null
    - checking types: use **typeof** to check what type a value is. (note: arrays return "object" because of JS)
- Template literals: 
    - old way: concatenation
    - new way: template literals
    - multi line strings
- Arithmetic operators: standard math works as expected. % (modulo) gives the remainder, ** is for exponents 
- string operations: built in methods for common transformations
- always use === for comparison operators
    - suggested: always use triple "=" because it is a strict equality check

## Code I'm proud of (Optional)
- I am proud of the fact that I was able to do the s16 demo almost completely by myself, only using AI to fix a mistake that I could not figure out. 

## Challenges I Faced

## AI Usage 
- I used AI to help me locate and separate my javascript code from my html and move it into a new javascript file in a new folder so that all my code looks cleaner and is easier to navigate. 
- I also used AI to 

## Questions for Going Forward
N/A



