# Session 18 & 19 - Mar 31 & Apr 2 - 

## What I Learned This Week
### Tuesday, Mar 31
- what is a function? 
    - A reusable block of code that performs a task (define the function and then execute it)
    - Parameters let functions work with different inputs each time you call them.
- basic if:
    - The condition must be in parentheses. The code block runs only when the condition is true.
- if else... conditions are checked in order so the first true condition runs and the rest are skipped
#### Review Questions
- you wrote const total = price + tax and got "1005" instead of 15. What happened? How do you fix it?
    - All about the type of inputs you have --> string
    - we can fix by creating another 2 variables so we can use a function that is called parseFloat --> converts a tyoe from string to number 
- Your code has const counter = 0 and later counter = counter + 1. It crashes. Why, and what's the one-word fix?
    - can't use 2 const variables, need to use "let" if you expect the variable to change in the future
- Rewrite this using a template literal: "Hello, " + name + "! You have " + count + " items."
    - make sure that the file is saved. (the backtick)
- Why does 5 == "5" return true but 5 === "5" returns false?
    - "===" is a strict equality. checks the value and
    - "==" although the values are the same, the '' makes it not the same

### Thursday, Apr 02
- falsy values: these six values are falsy. everything else (including the string "false") are true
- Ternary operator: Shorthand for if/else. use only for brief assignments, not complex logic
- Form validation: this checks that email exists and password meets minimum length before allowing submission 
- What is an array?
    - stores multiple values in a single variable 
    - searching arrays: includes() returns true/false. indexOf() returns the position
- array vs. string: strings are immutable (cannot be changed), arrays are mutable (can be changed)
#### Review Questions
- You wrote function getGrade(score) but calling getGrade(85) returns undefined. What's most likely missing inside the function?
    - a return function 
- Your function checks if (score >= 60) first, then else if (score >= 90). What would a student with 95 get? Why?
    - they would get a pass because the code is not well done. It is going through everything in the sequence that it is written in 
- What does this arrow function do? const f = x => x ** 2;
    - this is called an arrow function 
- You wrote let username = prompt("Enter your name"); and the user clicked OK without typing anything. Does if (username) run?
    - You can add something so that it greets you as a guest. Add an if else. If they do not enter a username, then it will automatically call them guest
    - in conclusion, it does run 

## Code I'm proud of (Optional)

## Challenges I Faced

## AI Usage 
- I used AI to help me with the changing of the colors based on the letter grade for the grade calculator because I ran into some issues when I tried to do it myself. 

## Questions for Going Forward
N/A



